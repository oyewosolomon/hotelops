export interface Feature {
    title: string;
    description: string;
    icon?: string; // URL or class name for icons
  }
  
  export interface Testimonial {
    name: string;
    role: string;
    quote: string;
    image?: string; // URL for testimonial image
  }